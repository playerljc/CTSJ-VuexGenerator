import VuexGenerator, {
  mapMutations,
  mapState,
  mapActions,
  useState,
  useMutations,
  useActions,
  cleanMixin,
} from './vuexgenerator';

export { mapState, mapMutations, mapActions, useState, useMutations, useActions, cleanMixin };

export default VuexGenerator;
